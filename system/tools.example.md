# Tools Example

Copy or rename this file to `<AR_MANAGEMENT_ROOT>/system/tools.md` when scaffolding a management root.

Use this file as the active list of CLI commands, MCPs, and other checks the agent should reference. The derived path locations are documented in `<AR_MANAGEMENT_ROOT>/system/settings.md`.

## Coding Tools

### Code Quality

#### Repo 1
- npm run lint
